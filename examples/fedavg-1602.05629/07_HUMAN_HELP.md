# 07 人工帮助

## 人工帮助清单

| ID | 需求 | 为什么需要 | 人工动作 | 是否阻塞 | 需要阶段 |
|---|---|---|---|---|---|
| H1 | 确认复现范围 | 大规模社交网络 word LSTM 数据闭源 | 决定只做公开数据复现，还是尝试申请私有数据 | 对 Figure 5 是阻塞 | S0 |
| H2 | GPU 预算 | 完整学习率 sweep 需要大量 run | 批准 GPU 型号、数量、最大运行时间/成本 | 对完整复现是阻塞 | S2-S7 |
| H3 | 框架偏好 | PyTorch 实用，但不与原始实现完全对齐 | 批准 PyTorch，或要求 TensorFlow 风格实现 | 编码前阻塞 | S2 |
| H4 | Shakespeare 许可证/引用 | 需要遵守 Project Gutenberg 条款 | 确认使用和引用方式 | 内部实验不阻塞，公开分发前需要 | S1 |
| H5 | 外网访问 | 需要下载数据和 arXiv/Gutenberg 内容 | 允许访问，或提供本地压缩包 | 数据下载阻塞 | S1 |
| H6 | 闭源社交网络数据 | 论文大规模 LSTM 使用不可用的作者/机构数据 | 提供数据/访问权限，或批准跳过 | 对 Figure 5 阻塞 | S5/S8 |
| H7 | 论文模糊点决策 | parser、seed、学习率 grid、框架都是重建决策 | 审阅 `05_ISSUES_AND_ASSUMPTIONS.md` 并批准 | 对最终 claim 阻塞 | S0-S3 |
| H8 | 计算截止策略 | 完全匹配表格可能很贵 | 决定可接受偏差和何时停止 sweep | 对预算控制阻塞 | S5-S7 |

## 需要马上确认的问题

1. 复现目标是否只包含公开数据：MNIST、Shakespeare、CIFAR-10？
2. 是否接受 PyTorch，还是要更贴近 2016 年 TensorFlow 实现？
3. 第一轮可用 GPU 预算是什么：CPU-only、本地单卡、还是远程 GPU？
4. 大规模 word-level LSTM 是否使用公开替代数据，还是将 Figure 5 保持为 BLOCKED？

## 非阻塞说明

- 公开数据复现不需要付费 API。
- 如果接受 PyTorch，不需要专有软件。
- 不要把任何凭据写入文件。如果之后提供私有数据，只挂载本地路径并记录路径，不提交数据本体。
